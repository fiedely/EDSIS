rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read access to everyone (for now)
      allow read: if true;
      // Only allow write if logged in (standard)
      allow write: if request.auth != null;
    }
  }
}